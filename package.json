{
  "name": "semir-yunpan-cli",
  "version": "0.1.0",
  "description": "森马云盘 CLI for users and AI agents",
  "type": "module",
  "bin": {
    "semir-yunpan": "dist/cli.js"
  },
  "scripts": {
    "dev": "tsx src/cli.ts",
    "build": "tsc -p tsconfig.json && chmod +x dist/cli.js",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "commander": "^14.0.3",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "@types/ws": "^8.5.13",
    "tsx": "^4.21.0",
    "typescript": "^6.0.2",
    "vitest": "^4.0.15"
  }
}
